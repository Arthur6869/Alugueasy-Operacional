
  # Consistent Design Token Application

  This is a code bundle for Consistent Design Token Application. The original project is available at https://www.figma.com/design/6dwHUwlBvVx5wzYGayvFhn/Consistent-Design-Token-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  